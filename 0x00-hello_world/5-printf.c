#include <stdio.h>
/**
 * main - prints a phrase
 * Return: 0 if executed successfully, no-zero otherwise
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
